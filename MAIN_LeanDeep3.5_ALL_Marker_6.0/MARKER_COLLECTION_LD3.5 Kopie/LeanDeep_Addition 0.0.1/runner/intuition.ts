import type { MarkerHit } from "./types";

export async function contextual_rescan(
  state: MarkerHit[]
): Promise<MarkerHit[]> {
  // Placeholder for CLU_INTUITION family-scoped multiplier
  return state;
}
